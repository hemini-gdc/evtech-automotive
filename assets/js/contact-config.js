/**
 * Contact form delivery — messages are emailed to recipientEmail.
 * Uses FormSubmit (https://formsubmit.co). On first use, check that inbox
 * and click the activation link FormSubmit sends (one-time setup).
 */
window.EVTECH_CONTACT = {
    recipientEmail: 'Info@evtech.co.nz',
};
